query Input {
  cart {
    cost {
      subtotalAmount {
        amount
      }
    }
    deliveryGroups {
      id
      deliveryOptions {
        handle
        cost {
          amount
        }
      }
    }
   buyerIdentity {
      customer {
        id
        hasTags(tags: [
          "tier: Defender"
        ]) {
          tag
          hasTag
        }
      }
    }
  }
  discount {
    metafield(namespace: "$app", key: "free-shipping-defender-level") {
      jsonValue
    }
  }
}