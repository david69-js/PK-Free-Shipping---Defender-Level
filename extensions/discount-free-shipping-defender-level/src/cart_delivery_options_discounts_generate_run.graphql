query Input {
  cart {
    cost {
      subtotalAmount {
        amount
      }
    }
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
        cost {
          amount
        }
      }
    }
   buyerIdentity {
      customer {
        id
        hasTags(tags: [
          "tier: Defender"
        ]) {
          tag
          hasTag
        }
      }
    }
  }
  discount {
    metafield(namespace: "$app", key: "loyalty-defender-level") {
      jsonValue
    }
  }
}